$schema: "http://json-schema.org/draft-06/schema#"

description: comparisons for edgeR
properties:

  name:
    type: string
    description: name of contrast

  meta_col:
    type: string
    description: name of meta_col containing relative and baseline identities

  relative:
    type: string
    description: the experimental group

  baseline:
    type: string
    description: the control group
